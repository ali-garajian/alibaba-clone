import MainWrapper from 'containers/global/MainWrapper';
import { Router } from 'routes';

function App() {
  return (
    <MainWrapper>
      <Router />
    </MainWrapper>
  );
}

export default App;
